var str: string = "asdas";
var num: number = 10.8;
var bool: boolean = true;

var obj: object = {a: 10, b: 20}

var mystr = "some string"

function createHelloString(name: string): string {
  return "Hello" + name
}
